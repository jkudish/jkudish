# Spec Summary (Lite)

Implement two GitHub Actions workflows to automate code quality and testing on pull requests. The Laravel Pint workflow will automatically format code and commit fixes back to PRs, while the Pest workflow will run all tests and post detailed failure reports as PR comments, ensuring consistent code quality without manual intervention.